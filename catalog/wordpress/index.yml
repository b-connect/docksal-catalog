title: Default Wordpress Setup
description: Default Wordpress stack
variables:
  VIRTUAL_HOST:
    default: wordpress.docksal
    title: Default url
    description: Set a url for access your project
  DOCROOT:
    default: docroot
    title: Default docroot path
    description: Set the project docroot path for your project
