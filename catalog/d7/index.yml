title: Default D7 Setup
description: Default drupal 7 stack
variables:
  VIRTUAL_HOST:
    default: d7.docksal
    title: Default url
    description: Set a url for access your project
  DOCROOT:
    default: docroot
    title: Default docroot path
    description: Set the project docroot path for your project
