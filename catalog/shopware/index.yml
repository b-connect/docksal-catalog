title: Default Shopware Setup
description: Default Shopware stack
variables:
  VIRTUAL_HOST:
    default: shopware.docksal
    title: Default url
    description: Set a url for access your project
  DOCROOT:
    default: docroot
    title: Default docroot path
    description: Set the project docroot path for your project
